{
  "name": "chat-app-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev",
    "dev:server": "npm --workspace apps/server run dev",
    "dev": "npm-run-all -p dev:server dev:web || (echo Use two terminals if npm-run-all is missing)",
    "build": "npm --workspace apps/server run build && npm --workspace apps/web run build",
    "start": "npm --workspace apps/server start"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "firebase": "^10.14.1"
  }
}
